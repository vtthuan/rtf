{% extends 'base.html.twig' %}

{% block main %}   
    <div class="main-grids">
        {%render(controller('AppBundle:Category:recent'))%}
        {% for category in categories %}
            {{ include('category/category_detail.html.twig', { 'category': category, 'language': language}) }}    
        {% endfor %}
    </div>    
{% endblock %}